import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sophisticated.pos',
  appName: 'Sophisticated POS',
  webDir: 'dist'
};

export default config;
